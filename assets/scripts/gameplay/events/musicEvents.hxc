function startMusic(track:String)
{
	PlayState.startMusic(track);
}

function stopMusic()
{
	FlxG.sound.music.stop();
}

function pauseMusic()
{
	if (FlxG.sound.music.playing)
		FlxG.sound.music.pause();
}

function unpauseMusic()
{
	if (!FlxG.sound.music.playing)
		FlxG.sound.music.resume();
}
