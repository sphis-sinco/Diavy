function gameplay_setDialogue()
{
	PlayState.instance.deleteAllDialogue();
	PlayState.instance.addDialogueJsonPath('start');
}

function nextDialogue()
{
	/**
	 * Was gonna check for the right progress index and perform shit, but then I added the `script_event` field :/
	 */
	/**
	var progress = PlayState.instance.dialogue_progress;

	switch(progress)
	{
		case 
	}
	 */
}