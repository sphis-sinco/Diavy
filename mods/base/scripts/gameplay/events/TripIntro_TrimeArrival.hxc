function tripIntro_trimeArrival(fuRoute:Bool)
{
	if (fuRoute)
	{
		PlayState.instance.addDialogueJsonPath('scenes/trip/intro/trimeArrival-fu');
	}
	else
	{
		PlayState.instance.addDialogueJsonPath('scenes/trip/intro/trimeArrival');
	}
}
